// Fix: Use named type import to resolve resolution error for NextConfig
import { type NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;